# AIMS monorepo

Amsol Integrated Management System (AIMS). `frontend/` is a TanStack Start + React app; `backend/` is a NestJS + MySQL (Prisma) API. Each has its own package.json and is developed/deployed independently.
